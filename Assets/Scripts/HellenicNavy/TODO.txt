__COMPLETED__
- create accounts (1x user, 1x supervisor, 1x admin) (done)

- add change password functionality (done)

- add password to database (and change password) (needs new dll)

- loading message (done - message box)

- limit hours/days input to comma separated ints only (done)(it uses newlines but w/e same difference)(each entry only has 1 value for days/hours now)

- add a way to add the assignment types (done)

- change how input fields are shown in the add service entry (maybe not)(done)

- choosing department button should open a dialogue to change the department > 
			choosing YES should load all systems (button array) > 
			each button should load all machinery in that system (button array) (done)

- read/write json for service entry (add close button to interface) (interface should close and reset on logout) (done)
			(write works but all , are turned to . just need to make sure you change back after reading) (works)

- each dept should have its own systems -> database should be changed (done)

- when adding machinery, the avaialble systems should load after picking dept (done - picking system automatically adds the dept)

- dictionary with depts and systems should be populated on start + when new system/dept is added (done)

- fix weird ui behavior (force update layout immediate does not work) 
	(this is weird and still doesnt work properly, workaround is to have all panels enabled before starting and disable them in awake)
	(some panels need to be enabled, some disabled but it's mostly fixed) (ui behavior is mostly fixed)

- add button for updating info on machinery (done)

- disable ui elements depending on user (done 2/3)

- add edit and fix other buttons on display machinery interface (done)

- redo TODO text (done)

- added date time and checkers (thanks c#) (done)

- added a bunch of checks for when adding/deleting things (done)

- update machinery list on system interface after updating info (done, just goes back to depts)

- on start, after login(? maybe before) should search for all services that need to be done and notify (done with button, cause automatic would cause issues)

- figure out a generic way to show which services are pending, depending on the hours and the days (5th (lmao) iteration works (uses dynamic lookup table))
  consider changing this to check between lastServiceHours and currentServiceHours
- added settings class 

- add a way to mark a service as completed and one to postpone service, this should also change the last service hours/date 
  (done, selecting entries + pressing complete marks as complete and changes date - if there are services that havent been done (pending|postponed), they are will still pop up)

- should also highlight the service entries that need to be done -> add a way to mark service as done/postponed (done - pending = orangish, postoponed = redish, completed = white)

- updating the last service hours does not work properly (fixed)

- added service assignments, might need slight rework

- export/import database	(user presses export button -> dialogue to select folder -> copy file to folder) (done)
							(user presses import button -> dialogue to select file -> copy and replace to wherever the db is -> relink db manager) (done)
							(change default folder to be desktop for selecting folder to export, local disk C for importing database)
							(restrict to only show .db files)(or change file extension, barely matters anyway)(done)

- message box should disable buttons and inputs etc (this is not done in tne most elegant way but it works so done)

- reports (pdf) (partially done, need to do a better split on pages for better table creation + text box in the end for remarks) (done)
#################################################################################################################################################

__REMAINING__
- refactor database manager to command pattern (read write) (consider message box hide)(might omit entirely)
- user manual


#################################################################################################################################################
24/04/23 - 5323 lines of code